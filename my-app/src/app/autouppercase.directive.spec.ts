import { AutouppercaseDirective } from './autouppercase.directive';

describe('AutouppercaseDirective', () => {
  it('should create an instance', () => {
    const directive = new AutouppercaseDirective();
    expect(directive).toBeTruthy();
  });
});
